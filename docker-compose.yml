services:
  capauto:
    build: .
    container_name: capauto
    restart: unless-stopped
    env_file:
      - .env.local
    volumes:
      - capauto_data:/app/data
      - capauto_uploads:/app/public/uploads
    networks:
      - web
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=audit-app_web"
      - "traefik.http.routers.capauto.rule=Host(`capauto-maquette.alsek.fr`)"
      - "traefik.http.routers.capauto.entrypoints=websecure"
      - "traefik.http.routers.capauto.tls.certresolver=letsencrypt"
      - "traefik.http.services.capauto.loadbalancer.server.port=3000"
      - "traefik.http.routers.capauto-http.rule=Host(`capauto-maquette.alsek.fr`)"
      - "traefik.http.routers.capauto-http.entrypoints=web"
      - "traefik.http.routers.capauto-http.middlewares=capauto-redirect"
      - "traefik.http.middlewares.capauto-redirect.redirectscheme.scheme=https"

volumes:
  capauto_data:
  capauto_uploads:

networks:
  web:
    external: true
    name: audit-app_web
